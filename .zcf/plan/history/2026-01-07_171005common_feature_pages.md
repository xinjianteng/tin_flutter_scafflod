Task: common_feature_pages
Workflow: execution plan

Context:
- Add common feature pages with GetX, mock data, and componentized UI.

Steps:
1) Add routes + page modules for login sms, list, setting, profile detail, feedback, about, search.
2) Add models and mock data sources + repositories.
3) Add business components (otp login form, list item card, feedback form).
4) Implement page logic/state/view with refresh/search/forms.
5) Update strings/constants and README notes.
