# Tin Flutter 脚手架

基于 GetX、Flutter Smart Dialog、ScreenUtil、PullToRefresh 的多端脚手架。内置欢迎页、登录状态示例、底部导航与常用工具，适合作为跨端模板起步。

## 快速开始
1. 安装依赖
   ```bash
   flutter pub get
   ```
2. 运行
   ```bash
   flutter run
   ```
3. 发布前请根据目标端裁剪依赖、配置签名/包名/图标。

## 跨端依赖指引
| 能力/插件 | 移动 | 桌面 | Web | 说明 |
| --- | --- | --- | --- | --- |
| `permission_handler` 等原生插件 | ✅ | ✅ | 🚫 | 仅原生端可用；Web 构建请移除相关依赖或增加条件分支 |
| `sqflite` / `sqflite_common_ffi` | ✅ | ✅ | 🚫 | Web 推荐改用 `indexed_db`/`hive`；未使用时可注释掉依赖 |
| GetX、SmartDialog、ScreenUtil、纯 Dart 库 | ✅ | ✅ | ✅ | 全端可用；直接复用即可 |

- 针对**只做 Web**的场景，可以注释掉 `permission_handler`、`sqflite*`，重新执行 `flutter pub get`。
- 在代码中使用 `PlatformUtil`（`lib/utils/platform_util.dart`）判断端侧，避免 Web 上调用原生 API。

## 目录
```
lib/
├── api/                    # 接口定义（待扩展）
├── models/                 # 数据模型（Oauth/User 等）
├── pages/                  # 页面模块
│   ├── application/        # 主壳+底部导航
│   ├── home/               # 首页示例与跨端指引
│   ├── search/             # 搜索页占位
│   ├── profile/            # 个人中心（模拟登录）
│   ├── login/              # 登录页示例
│   ├── setting/            # 设置页示例
│   ├── about/              # 关于页
│   ├── not_found/          # 404 占位
│   └── welcome/            # 欢迎页倒计时
├── routers/                # 路由、中间件
├── service/                # 全局服务（OAuth 持久化等）
├── utils/                  # 工具（日志、偏好存储、加解密、平台判断等）
├── values/                 # 颜色、尺寸、文案常量
├── init_app.dart           # 启动初始化（Prefs/Oauth 注册）
└── main.dart               # 入口，挂载刷新配置、路由与日志
assets/
└── images/                 # 欢迎页/Logo 占位
```

## 已内置
- GetX 路由与依赖注入，欢迎页中间件根据首启/登录态跳转。
- ScreenUtil 适配、SmartDialog 弹窗、PullToRefresh 全局头尾配置。
- OauthService 持久化登录状态；Profile/Login 页提供模拟登录示例。
- PrefsUtil 统一偏好存储访问；Logger 简单日志输出。
- 文案/配色/尺寸集中在 `lib/values/`，方便统一定制。

## 开发命令
- 格式化：`dart format lib test`
- 分析：`flutter analyze`
- 测试：`flutter test`

## 替换建议
- 将 `assets/images/` 中的占位图替换为真实资源，或按需精简。
- 按领域在 `pages/` 与 `routers/pages.dart` 添加新模块，使用 `AppRoutes` 管理路由常量。
- 根据实际业务实现登录接口，替换 `LoginLogic.mockLogin` 与 `OauthService` 的持久化逻辑。
