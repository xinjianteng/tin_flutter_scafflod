# Tin Flutter 脚手架

基于 GetX、Flutter Smart Dialog、ScreenUtil、PullToRefresh 的多端脚手架。内置欢迎页、登录状态示例、底部导航与常用工具，适合作为跨端模板起步。

## ✨ 特性

- 🎯 **完整的架构分层**：DataSource → Repository → Service → Logic → View
- 🎨 **丰富的组件库**：原子组件、通用组件、业务组件
- 🔧 **开箱即用**：登录、注册、列表、详情、通知等常用页面
- 🌍 **多端支持**：Android、iOS、HarmonyOS、Web、桌面
- 📱 **响应式设计**：基于 ScreenUtil 的屏幕适配
- 🎭 **状态管理**：GetX 响应式状态管理
- 🛠️ **开发工具**：环境配置、错误处理、日志系统

## 📚 文档

- [快速开始](./docs/QUICK_START.md) - 5分钟上手指南
- [常见问题](./docs/FAQ.md) - 开发中的常见问题解答
- [CLAUDE.md](./CLAUDE.md) - AI 辅助开发指南

## 快速开始

### 1. 安装依赖
```bash
flutter pub get
```

### 2. 运行项目
```bash
flutter run
```

### 3. 查看组件展示
运行项目后，进入 **设置 → 组件展示** 查看所有可用组件。

## 跨端依赖指引
| 能力/插件 | 移动 | 桌面 | Web | 说明 |
| --- | --- | --- | --- | --- |
| `permission_handler` 等原生插件 | ✅ | ✅ | 🚫 | 仅原生端可用；Web 构建请移除相关依赖或增加条件分支 |
| `sqflite` / `sqflite_common_ffi` | ✅ | ✅ | 🚫 | Web 推荐改用 `indexed_db`/`hive`；未使用时可注释掉依赖 |
| GetX、SmartDialog、ScreenUtil、纯 Dart 库 | ✅ | ✅ | ✅ | 全端可用；直接复用即可 |

- 针对**只做 Web**的场景，可以注释掉 `permission_handler`、`sqflite*`，重新执行 `flutter pub get`。
- 在代码中使用 `PlatformUtil`（`lib/utils/platform_util.dart`）判断端侧，避免 Web 上调用原生 API。

## 目录
```
lib/
├── components/             # 组件分层
│   ├── atoms/              # 原子组件（按钮/输入框/文本/加载）
│   ├── common/             # 通用组件（弹窗/空状态/刷新列表）
│   └── business/           # 业务组件（登录表单/用户卡片）
├── api/                    # 接口定义（待扩展）
├── datasource/             # 数据源（远端/本地）
├── models/                 # 数据模型（Oauth/User 等）
├── pages/                  # 页面模块
│   ├── application/        # 主壳+底部导航
│   ├── home/               # 首页示例与跨端指引
│   ├── search/             # 搜索页占位
│   ├── profile/            # 个人中心（模拟登录）
│   ├── login/              # 登录页示例
│   ├── setting/            # 设置页示例
│   ├── about/              # 关于页
│   ├── not_found/          # 404 占位
│   └── welcome/            # 欢迎页倒计时
├── routers/                # 路由、中间件
├── repository/             # 仓库层（业务数据组合）
├── service/                # 全局服务（OAuth 持久化等）
├── utils/                  # 工具（日志、偏好存储、加解密、平台判断等）
├── values/                 # 颜色、尺寸、文案常量
├── init_app.dart           # 启动初始化（Prefs/Oauth 注册）
└── main.dart               # 入口，挂载刷新配置、路由与日志
assets/
└── images/                 # 欢迎页/Logo 占位
```

## 已内置
- GetX 路由与依赖注入，欢迎页中间件根据首启/登录态跳转。
- ScreenUtil 适配、SmartDialog 弹窗、PullToRefresh 全局头尾配置。
- OauthService 持久化登录状态；Profile/Login 页提供模拟登录示例。
- PrefsUtil 统一偏好存储访问；Logger 简单日志输出。
- 文案/配色/尺寸集中在 `lib/values/`，方便统一定制。
- 常用功能页面：手机号验证码登录、列表刷新、搜索、个人信息、反馈、设置、关于。

## 架构分层（推荐实践）
- View：`pages/*/*_view.dart` 负责页面布局与导航。
- ViewModel：`pages/*/*_logic.dart` 负责业务流程与状态协调。
- State/Model：`pages/*/*_state.dart` + `models/` 管理页面状态与数据模型。
- Repository：`repository/` 负责业务数据组合。
- DataSource：`datasource/` 负责远端/本地数据获取。
- 组件层级：`atoms -> common -> business -> pages` 逐层依赖，避免跨层引用。

## 开发命令
- 格式化：`dart format lib test`
- 分析：`flutter analyze`
- 测试：`flutter test`

## 替换建议
- 将 `assets/images/` 中的占位图替换为真实资源，或按需精简。
- 按领域在 `pages/` 与 `routers/pages.dart` 添加新模块，使用 `AppRoutes` 管理路由常量。
- 根据实际业务实现登录接口，替换 `LoginLogic.mockLogin` 与 `OauthService` 的持久化逻辑。
